# Bash Loops Notes

## What is a loop?

A loop repeats commands.

Instead of typing the same command many times, I can use a loop.

## For loop

A `for` loop goes through a list of items.

Example:

```bash
for ITEM in user date uptime
do
  echo "$ITEM"
done
